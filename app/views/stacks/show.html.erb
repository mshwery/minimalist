<div class="list">
  <h1>My lists</h1>
  <ul id="my-lists" class="items">
    <% @lists.each do |list| %>
      <li>
        <%= link_to stack_list_url(@stack, list) do %>
          <%= list.name %> 
          <%= content_tag(:i, list.remaining.length) %>
        <% end %>
        <%#= link_to "Delete", stack_list_url(@stack, list), :method => :delete %>
      </li>
    <% end %>
  </ul>
</div>

<%= link_to "Make a list", new_stack_list_url(@stack) %>
